spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql:///blog_project?characterEncoding=UTF-8&useSSl=false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    com.kyo: debug
  file:
    name: log/blog-dev.log